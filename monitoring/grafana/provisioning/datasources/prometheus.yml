apiVersion: 1

# Auto-provisions Prometheus as the default datasource so dashboards work on first boot.
datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 15s
